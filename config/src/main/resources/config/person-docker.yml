spring:
  jpa:
    database: POSTGRESQL
  datasource:
    platform: postfres
    url: jdbc:postgresql://postgres:5432/postgres
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver